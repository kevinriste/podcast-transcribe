[project]
name = "rss"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "feedparser",
    "python-dateutil",
    "beautifulsoup4",
    "waybackpy",
    "trafilatura",
    "playwright",
    "msgspec",
]

[dependency-groups]
dev = []

[tool.uv.sources]
